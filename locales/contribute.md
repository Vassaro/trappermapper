# Contribute

To add a new language, do the following:

- copy one of the json files in `/locales/` and name it to your new language, eg. `de.json`.
- in `/js/data`, add the new locale with translations in `layers.json`, `layers.example.json` and all the `.geojson` files.